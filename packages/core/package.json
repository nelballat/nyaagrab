{
  "name": "@nyaagrab/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@nyaagrab/contracts": "workspace:*"
  },
  "devDependencies": {
    "@nyaagrab/test-fixtures": "workspace:*"
  }
}
